import { tokens, themeTokens, colors, spacing, radii, typography, shadows, glassmorphism } from '../theme';

export { tokens, themeTokens, colors, spacing, radii, typography, shadows, glassmorphism };

export const theme = {
  ...themeTokens,
};

export default theme;
